<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hr.mappers.ManagerMapper">
	<select id="getMana" resultType="ManaVO">
		SELECT groupName,groupId,groupSq,startDate,endDate,groupType,groupCount From manager
	</select>
	<delete id="deleteEmp">
		DELETE 
		FROM manager
		WHERE id = #{id};
	</delete>
	<select id="get">
		
	</select>
</mapper>